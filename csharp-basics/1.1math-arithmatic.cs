namespace math {
    public class arithmatic {

        void example() {


const bool eternal = true;                  // Variable Constant

Console.Write("what is your age: ");        // User Input
string age = Console.ReadLine();            

Console.Write(age.GetType());               // Display type

double newAge = Convert.ToDouble(age);      // Type Casting
Console.Write(newAge.GetType());

int a = 1;
int b = 2;                                  // Arithmatic Operators
int c = a + b;                              // Addition
int d = b - 1;                              // Subtraction
int e = b * 2;                              // Multiplication
int f = b / 3;                              // Division

Double x = 1.1;
Math.Pow(b, 2);                            // Squared
Math.Sqrt(e);                              // Square Root
Math.Abs(a);                               // Absolute Value
Math.Round(x);                             // Round
Math.Ceiling(x);                           // Round Up
Math.Floor(x);                             // Round Down

Console.ReadKey();


        }

    }
}